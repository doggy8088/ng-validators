import { CompareEqualDirective } from './compare-equal.directive';

describe('CompareEqualDirective', () => {
  it('should create an instance', () => {
    const directive = new CompareEqualDirective();
    expect(directive).toBeTruthy();
  });
});
